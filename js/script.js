if (window.SimpleSlide) {
  new SimpleSlide({
    slide: "quote",
    time: 5000,
  });

  new SimpleSlide({
    slide: "portfolio",
    nav: true,
    time: 5000,
    auto: true,
  });
}

if (window.SimpleAnime) {
  new SimpleAnime();
}
